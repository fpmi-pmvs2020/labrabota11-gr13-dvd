<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/Theme.FBResult.AppBarOverlay">

    <LinearLayout
        android:id="@+id/duty_frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/textView3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:foreground="@color/dark_blue"
            android:gravity="center"
            android:text="fddsgsfgh"
            android:textColor="@color/purple_200"
            android:textSize="36sp" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/duty_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/duty_frame"></androidx.recyclerview.widget.RecyclerView>

</RelativeLayout>